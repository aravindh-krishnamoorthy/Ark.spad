++
++ Conjugate transpose
++
ctranspose(X) ==
    _Y := transpose(X)
    for n in rowSlice(_Y) repeat for m in colSlice(_Y) repeat _Y(m,n) := conjugate(_Y(m,n))
    _Y

++
++ Identity matrix
++
eye(n) == diagonalMatrix [1 for i in 1..n]

++
++ Singular values
++
singularvalues(X) ==
    _sv := []
    _e := solve(X*ctranspose(X) - _s*eye(nrows(X)),_s)
    for _i in 1..#_e repeat _sv := append(_sv, sqrt(_e(_i)))
    _sv
